# ENH: consider a noise-tolerant limited-memory regularized quasi-Newton solver

Hello, SciPy maintainers.
I'm Hiroki Hamaguchi, a doctoral student working in numerical optimization.

I greatly appreciate your work on SciPy, and I would like to discuss a possible
addition to `scipy.optimize.minimize`, motivated in part by observations about
its L-BFGS-B implementation.

(I previously presented this work at
[SciPyData 2026](https://scipydata.connpass.com/event/364718/) in Japan.)

This issue has two related motivations:

1. On some problems with inexpensive function and gradient evaluations, the
   current L-BFGS-B implementation has a relatively large wall-clock cost per
   iteration.
2. Recent regularized quasi-Newton methods provide an alternative to the
   traditional line-search L-BFGS framework and may be faster or more robust on
   some problems.

I therefore propose adding a new, Python-based, limited-memory regularized
quasi-Newton method as an additional `minimize` method.

In this issue, I would like to explain the motivation and ask whether this
would be a reasonable direction for SciPy.

## Motivations

### Per-iteration wall-clock cost

The first motivation is the overhead observed when the objective and gradient
are inexpensive to evaluate.

SciPy's current implementation is a C translation of L-BFGS-B, exposed through
Python by [`minimize(method="L-BFGS-B")`](https://docs.scipy.org/doc/scipy/reference/optimize.minimize-lbfgsb.html) ([current C source](https://github.com/scipy/scipy/blob/main/scipy/optimize/src/lbfgsb.c)).

I do not yet have a complete diagnosis, but in our experiments we observe a
relatively high cost per iteration compared with several Python-based
limited-memory methods.

The following experiment is Figure 3 of our
[paper](https://arxiv.org/abs/2603.10642). The plotted values are total solver
time for the same iteration budget, so they also give a direct comparison of
mean cost per iteration. The paper's experiments were run on Windows 11 with an
Intel Core i7-1360P CPU.

![Execution time for 100 iterations on a cheap quadratic](https://raw.githubusercontent.com/HirokiHamaguchi/qnlab/main/doc/SciPy/time_per_iteration.png)

In this experiment, the C-based L-BFGS-B implementation is slower than several
Python-based limited-memory methods. One possible contributor is overhead at
the Python/C boundary, although the current evidence does not establish a
single cause. After several attempts to reduce the observed overhead, we have
not been able to eliminate it. The effect may depend on the platform and
problem dimensions.

[`notebooks/SciPy_issue.ipynb`](https://github.com/HirokiHamaguchi/qnlab/blob/main/notebooks/SciPy_issue.ipynb)
is a separate reproduction and diagnosis notebook with further details.
The proposed method is not always faster than the current C implementation,
but it is faster on many of the tested problems.

This is one of the motivations for proposing a new solver.
A Python implementation would also make the new algorithm easier to review,
experiment with, and maintain. Our measurements suggest that such an
implementation can still be competitive in wall-clock time for some problems.

### Recent progress in quasi-Newton methods

The second motivation is recent progress in quasi-Newton methods. SciPy's
current L-BFGS-B implementation is sophisticated, stable, and unquestionably
important. Recent research has also produced complementary approaches that may
benefit SciPy users, especially for problems with inaccurate function values.

The method we would like to propose is the noise-tolerant regularized
quasi-Newton method (NTRQN) described in
[*Practical Regularized Quasi-Newton Methods with Inexact Function Values*](https://arxiv.org/abs/2603.10642).

This is a method from our own paper, and one of our goals is to contribute the
result to SciPy. We welcome an independent assessment of whether it fits
SciPy's scope and would not pursue the proposal if the maintainers consider it
unsuitable. Although the particular combination is ours, many of its components
build on developments from the numerical optimization literature over the past
several decades:

- **Regularized quasi-Newton directions:** compute a direction corresponding
  to $-(B_k+\mu_k I)^{-1}g_k$, following regularized Newton and limited-memory
  RQN work ([Ueda and Yamashita, 2014](https://doi.org/10.1007/s10589-014-9656-x);
  [Kanzow and Steck, 2023](https://doi.org/10.1007/s12532-023-00238-4)). Compared
  with line-search L-BFGS, this approach can be more robust or faster on some
  problems.
- **Curvature safeguards independent of Wolfe success:** damped BFGS and
  cautious selection of stored pairs maintain usable, spectrally bounded
  limited-memory approximations. This follows the cautious-update tradition
  ([Li and Fukushima, 2001](https://doi.org/10.1137/S1052623499354242)).
- **Optional modified secant information:** when function values are reliable,
  their interpolation information can improve the curvature model, following
  the modified-secant literature ([Zhang, Deng and Chen, 1999](https://doi.org/10.1023/A:1021898630001);
  [Yabe, Ogasawara and Yoshino, 2007](https://doi.org/10.1016/j.cam.2006.05.018)).
  Roughly speaking, this incorporates function-value interpolation, of the kind
  also used by the Moré--Thuente line search in L-BFGS-B, into the quasi-Newton
  model update itself.
- **Use function values only when they are informative:** a relaxed,
  non-monotone Armijo test keeps ordinary quasi-Newton behavior when sufficient
  progress is observable. Otherwise, positive regularization is controlled
  by accumulated gradient norms, following the Objective-Function-Free
  Optimization (OFFO)/AdaGrad-Norm line of work
  ([Gratton, Jerad and Toint, 2024](https://doi.org/10.1080/10556788.2023.2296431)).

## Benchmarking

[CUTEst](https://doi.org/10.1007/s10589-014-9687-3) is a standard collection and
testing environment for optimization problems.
We conducted tests on 220 unconstrained problems at 64-bit precision and 124
at 32-bit precision.
The limited-memory methods used $m=10$, and the maximum iteration count was
15,000.

The primary figures use Dolan--Moré
[performance profiles](https://doi.org/10.1007/s101070100263). For problem $p$
and solver $s$, the ratio is

$$
r_{p,s}=\frac{n_{p,s}}{\min_{s'} n_{p,s'}},
$$

where $n_{p,s}$ is the number of oracle calls required to reach the stated
gradient tolerance (and is infinite on failure).
At $\tau=1$, the height is the fraction of problems on which a solver was best.
At larger $\tau$, the height is the fraction solved within $\tau$ times the best
solver's oracle count.
The final height therefore indicates robustness/coverage.

| 64-bit, $\|g\|_\infty\le10^{-5}$ | 32-bit, $\|g\|_\infty\le10^{-3}$ |
| :---: | :---: |
| ![64-bit performance profile](https://raw.githubusercontent.com/HirokiHamaguchi/qnlab/main/doc/imgs/compare/_pp_precision64_gtol1e-05.png) | ![32-bit performance profile](https://raw.githubusercontent.com/HirokiHamaguchi/qnlab/main/doc/imgs/compare/_pp_precision32_gtol1e-03.png) |

![Legend for performance profiles](https://raw.githubusercontent.com/HirokiHamaguchi/qnlab/main/doc/imgs/compare/_legend.png)

All 564 available per-problem traces, including wall-clock plots, are collected in
[`cutest_individual_results.pdf`](https://github.com/HirokiHamaguchi/qnlab/blob/main/doc/SciPy/cutest_individual_results.pdf).
The document is reproducibly generated from the original plots by
[`generate_issue_assets.py`](https://github.com/HirokiHamaguchi/qnlab/blob/main/doc/SciPy/generate_issue_assets.py).

Within this benchmark, the proposed variants are competitive in oracle calls
and per-iteration cost and solve a high fraction of the test problems. This
statement is limited to the reported benchmark, settings, and competitors;
quasi-Newton performance is problem-dependent, and there are problems on which
L-BFGS-B is faster or more accurate.

## Box constraints (the "-B" in L-BFGS-B)

The method can be extended to box constraints, which would be necessary for a
full L-BFGS-B replacement.

A working prototype,
[`qnlab/solver/qn_ntrqnb.py`](https://github.com/HirokiHamaguchi/qnlab/blob/main/qnlab/solver/qn_ntrqnb.py),
combines the regularized compact L-BFGS model with a generalized Cauchy point.

However, I do not currently plan to include the box-constrained variant.
It would add substantial implementation and maintenance cost, particularly if
an efficient production implementation eventually required compiled code.
Box support could be considered later if there is sufficient user and
maintainer interest.

## Questions for SciPy maintainers

Thank you for reading this issue.
I would particularly appreciate feedback on the following points:

1. Do you think that a new, Python-based, limited-memory regularized
   quasi-Newton method is a reasonable addition to SciPy's `minimize`?
2. If so, do you have any suggestions for the API, naming, or other design
   considerations?

I will follow SciPy's development guidelines and best practices and will be
responsive to maintainer feedback. I am prepared to provide the implementation,
tests, documentation, benchmarks, and follow-up maintenance if the proposal is
accepted.

Thank you for considering the proposal.
